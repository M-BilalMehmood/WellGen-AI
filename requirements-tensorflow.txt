# TensorFlow-specific requirements for RTX 5060 setup
# Use with Python 3.10

# Core TensorFlow (already installed in your global env)
# tensorflow>=2.15.0

# Keras NLP for GPT-2 and text models
keras-nlp>=0.6.0
keras-cv>=0.6.0

# For image generation with TensorFlow
# tensorflow-hub>=0.15.0

# If you want to try mixed approach (some models in PyTorch)
# torch>=2.0.0  # Only if you can install in venv
# transformers>=4.35.0

# Existing requirements (compatible with both)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
Pillow>=10.0.0
streamlit>=1.28.0
python-dotenv>=1.0.0
tqdm>=4.66.0
